// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "linux-musl", "linux-musl-openssl-3.0.x"]
  output        = "../node_modules/.prisma/client"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Opd {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  namaOpd   String
  
  userId    Int?     @unique
  user      User?    @relation(fields: [userId], references: [id])

  // Tambahkan ini agar OPD tahu jabatan apa saja yang dimilikinya
  jabatans  Jabatan[]
  
  // Tambahkan ini jika Pegawai punya relasi langsung ke OPD
  pegawai   Pegawai[]
}

model Jabatan {
  id           Int      @id @default(autoincrement())
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  namaJabatan  String
  level        Int      @default(1)

  // --- KOLOM LAMA JANGAN DIHAPUS ---
  kJ           Int      @default(0)  // Kelas Jabatan tetap di sini!
  b            Int      @default(0)  // BEZ
  aBK          Float    @default(0.00) // ABK
  kurangLebih  Int      @default(0)

  // --- TAMBAHAN HIRARKI ---
  parentId     Int?     
  parent       Jabatan?  @relation("JabatanHierarki", fields: [parentId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  children     Jabatan[] @relation("JabatanHierarki")

  // --- RELASI OPD ---
  opdId        Int?      @default(1) // Tambahkan default sementara agar tidak error saat migrate
  opd          Opd?       @relation(fields: [opdId], references: [id])

  pegawai      Pegawai[]
  tugas        Tugas[]
}

model Pegawai {
  id              Int      @id @default(autoincrement())
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  nama            String
  nip             String?   @unique // Tambahkan unique untuk keamanan data
  tanggalLahir    DateTime?
  tempatLahir     String?
  tahunPensiun    Int      @default(0)
  tahunKebutuhan  Int      @default(0)

  pendidikanId    Int?
  pendidikan      Pendidikan? @relation(fields: [pendidikanId], references: [id], onDelete: SetNull)
  
  jabatanId       Int?
  jabatan         Jabatan?    @relation(fields: [jabatanId], references: [id], onDelete: SetNull)

  // HAPUS bagian atasanId dan bawahanId di sini
  
  // Relasi OPD biasanya cukup lewat Jabatan saja, 
  // tapi jika ingin tetap ada relasi langsung:
  opdId           Int?
  opd             Opd?        @relation(fields: [opdId], references: [id])
}

model Tugas{
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  namaTugas String?
  hasil String?
  jumlahBebanKerjaSetahun Int @default(0)
  waktuPenyelesaianDalamJam Int @default(0)
  waktuEfektifPenyelesaian Int @default(0)
  KebutuhanPegawai Float @default(0.00)
  jabatanId Int
  jabatan Jabatan @relation(fields: [jabatanId], references: [id])
}

model Pendidikan {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  namaPendidikan  String
  pegawai Pegawai[]
}


model User {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  // username String
  // password String
  clerkUserId String? @unique // dari Clerk

  role String? // ADMIN_INDUK | ADMIN_OPD

  opd Opd?
}









// ################ backup tabel versi lama

// model pegawai versi lama
// model Pegawai {
//   id        Int      @id @default(autoincrement())
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt
//   nama  String
//   nip  String
//   tanggalLahir  DateTime
//   tempatLahir  String
//   tahunPensiun Int @default(0)
//   tahunKebutuhan Int @default(0)
//   pendidikanId Int?
//   pendidikan Pendidikan? @relation(fields: [pendidikanId], references: [id], onDelete: SetNull)
//   jabatanId Int?
//   jabatan Jabatan? @relation(fields: [jabatanId], references: [id], onDelete: SetNull)
//   opd Opd[]


//   // Self-referencing relationship for hierarchy
//   atasanId       Int?       // ID of the superior
//   atasan         Pegawai?   @relation("Hierarki", fields: [atasanId], references: [id])
//   bawahan        Pegawai[]  @relation("Hierarki")
// }


// model jabatan versi lama
// model Jabatan {
//   id        Int      @id @default(autoincrement())
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt
//   namaJabatan  String
//   pegawai Pegawai[]
//   tugas Tugas[]

  
//   kJ Int @default(0)
//   b Int @default(0)
//   aBK Float @default(0.00)
//   kurangLebih Int @default(0)
  
// }

// model opd versi lama
// model Opd {
//   id        Int      @id @default(autoincrement())
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt

//   namaOpd String
  
//   userId Int? @unique
//   user User? @relation(fields: [userId], references: [id])

//   pegawaiId Int?
//   pegawai Pegawai? @relation(fields: [pegawaiId], references: [id])
// }